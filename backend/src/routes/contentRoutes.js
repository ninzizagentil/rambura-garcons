import { Router } from 'express';
import { authenticate, authorize, requirePermission } from '../middleware/auth.js';
import { asyncHandler, fail, ok } from '../utils/api.js';
import { publicList, publicDetail, adminList, adminCreate, adminUpdate, adminDelete, publishNews, getSettings, updateSettings, getHero, updateHero } from '../controllers/contentController.js';
import { submit, getApplications, getApplication, updateStatus, remove } from '../controllers/admissionController.js';
import { submitContact } from '../controllers/contactController.js';
import ContactMessage from '../models/ContactMessage.js';
import { validateBody } from '../middleware/validate.js';
const publicRouter = Router();
publicRouter.post('/applications', validateBody({ fullName: { required: true, maxLength: 120 }, email: { required: true, email: true }, phone: { required: true, maxLength: 40 }, program: { required: true, maxLength: 120 }, message: { maxLength: 3000 } }), asyncHandler(submit)); publicRouter.post('/contact', validateBody({ name: { required: true, maxLength: 120 }, email: { required: true, email: true }, subject: { required: true, maxLength: 160 }, message: { required: true, maxLength: 5000 } }), asyncHandler(submitContact)); publicRouter.get('/settings', asyncHandler(getSettings)); publicRouter.get('/home', asyncHandler(getHero)); publicRouter.get('/:resource/:slug', asyncHandler(publicDetail)); publicRouter.get('/:resource', asyncHandler(publicList));
const adminRouter = Router(); adminRouter.use(authenticate, authorize('admin', 'management')); adminRouter.get('/contact-messages', asyncHandler(async (_req, res) => ok(res, await ContactMessage.find().sort({ createdAt: -1 }).limit(100)))); adminRouter.delete('/contact-messages/:id', asyncHandler(async (req, res) => { const message = await ContactMessage.findByIdAndDelete(req.params.id); return message ? ok(res, { id: message._id }, 'Contact message deleted') : fail(res, 'Contact message not found', 404); })); adminRouter.get('/settings', asyncHandler(getSettings)); adminRouter.put('/settings', asyncHandler(updateSettings)); adminRouter.get('/website/hero', asyncHandler(getHero)); adminRouter.put('/website/hero', asyncHandler(updateHero)); adminRouter.post('/news/:id/publish', asyncHandler((req, res) => publishNews({ ...req, body: { ...req.body, published: true } }, res))); adminRouter.post('/news/:id/unpublish', asyncHandler((req, res) => publishNews({ ...req, body: { ...req.body, published: false } }, res))); adminRouter.get('/:resource', asyncHandler(adminList)); adminRouter.post('/:resource', asyncHandler(adminCreate)); adminRouter.put('/:resource/:id', asyncHandler(adminUpdate)); adminRouter.delete('/:resource/:id', asyncHandler(adminDelete));
const applicationRouter = Router(); applicationRouter.use(authenticate); applicationRouter.get('/', requirePermission('applications.view'), asyncHandler(getApplications)); applicationRouter.get('/:id', requirePermission('applications.view'), asyncHandler(getApplication)); applicationRouter.patch('/:id/status', requirePermission('applications.update'), asyncHandler(updateStatus)); applicationRouter.delete('/:id', requirePermission('applications.update'), asyncHandler(remove));
export { publicRouter, adminRouter, applicationRouter };
